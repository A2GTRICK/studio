
/**
 * @fileOverview Central configuration for marketing-related settings.
 */

/**
 * The path to the lead magnet file that users receive upon subscribing to the newsletter.
 * This path should point to a file within the `public` directory.
 * 
 * To change the file, upload a new one to `public/assets` and update the string below.
 */
export const leadMagnetPath = '/assets/Top-20-Pharmacology-Questions.pdf';
