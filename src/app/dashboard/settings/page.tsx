// This file is now effectively replaced by the profile page. 
// We can redirect or just keep it empty, but for now we will just make it an alias for profile page.
import ProfilePage from "../profile/page";

export default function SettingsPage() {
    return <ProfilePage />;
}
