
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Notes and MCQ Sets are publicly readable.
    // Writes are disallowed from the client for security.
    match /notes/{noteId} {
      allow read: if true;
      allow write: if false;
    }

    match /mcqSets/{mcqId} {
      allow read: if true;
      allow write: if false;
    }

    // custom_notifications are publicly readable for all users.
    // Writes are only allowed by authenticated admins.
    match /custom_notifications/{notifId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    // Admin config is readable for the client-side admin login page to verify the code.
    // Update is restricted to authenticated admins.
    match /adminConfig/{doc} {
      allow read: if true;
      allow update: if request.auth != null && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
      allow create, delete: if false;
    }

    // Default deny: block all other access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
